import { DateHoursToTimePipePipe } from './date-hours-to-time-pipe.pipe';

describe('DateHoursToTimePipePipe', () => {
  it('create an instance', () => {
    const pipe = new DateHoursToTimePipePipe();
    expect(pipe).toBeTruthy();
  });
});
